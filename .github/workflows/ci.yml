name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        python: ["3.10", "3.11", "3.12", "3.13"]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
      - name: Unit and release-hygiene tests
        env:
          PYTHONDONTWRITEBYTECODE: "1"
        run: python tests/run_suite.py
      - name: Launcher smoke test
        shell: pwsh
        run: ./scripts/google-analytics.ps1 doctor --json
